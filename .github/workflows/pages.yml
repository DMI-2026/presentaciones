name: Deploy slides to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build every deck under semana-*/ with Marp CLI
        run: |
          set -e
          mkdir -p dist
          shopt -s nullglob
          for f in semana-*/*.md; do
            dir="dist/$(dirname "$f")"
            mkdir -p "$dir"
            npx --yes @marp-team/marp-cli@latest "$f" -o "$dir/index.html" --html
          done

      - name: Build landing page listing all decks
        run: |
          {
            echo '<!doctype html><meta charset="utf-8"><title>Presentaciones DMI-2026</title>'
            echo '<h1>Presentaciones — Desarrollo Móvil Integral</h1><ul>'
            for d in dist/semana-*/; do
              name=$(basename "$d")
              echo "<li><a href=\"$name/\">$name</a></li>"
            done
            echo '</ul>'
          } > dist/index.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
